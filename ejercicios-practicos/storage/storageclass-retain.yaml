# storageclass-retain.yaml
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: fast-storage
provisioner: k8s.io/minikube-hostpath # Sustituye si usas otro provisioner
reclaimPolicy: Retain
volumeBindingMode: Immediate